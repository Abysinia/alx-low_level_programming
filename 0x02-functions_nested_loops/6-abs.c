#include "main.h"
/**
 * _abs - absolute balue of the number from 0
 * @i: the integer to be covered.
 * Return: absolute balue of the input #
 */
int _abs(int i)
{
if (i < 0)
i = i * -1;
return (i);
}
